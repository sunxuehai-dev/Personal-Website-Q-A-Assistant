services:
  resume-assistant:
    container_name: resume-assistant
    build:
      context: .
      args:
        BASE_IMAGE: ${BASE_IMAGE:-python:3.11-slim}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-}
    image: ${IMAGE_NAME:-crpi-2rkpbd2bkf79ckyj.cn-hangzhou.personal.cr.aliyuncs.com/sunxuehai/github:latest}
    env_file:
      - .env
    ports:
      - "8008:8008"
    volumes:
      - ./data:/app/data
    restart: always
