# Resume Assistant 开发记录

日期：2026-04-14

## 1. 当前项目阶段

当前项目已经从“功能可演示、部署链已打通”的状态，继续推进到了：

**后端工程化规则进一步收口，个人简历知识库更新流程已标准化，本地与服务器数据状态已同步。**

这意味着项目现在不仅具备：

- 本地运行能力
- GitHub 同步能力
- 服务器 Compose 部署能力
- 双知识库问答能力

还进一步补上了“自有简历如何稳定更新、旧数据如何清理、线上如何同步最新简历”的工程化细节。

---

## 2. 今天的工作重心

今天的工作重点没有继续扩展产品功能，而是集中在后端工程化与标准化收口，主要解决了三类问题：

1. 旧脚本与旧文档入口仍然存在，容易误导后续维护
2. `self_resume` 的灌库流程语义不够严格，更新新简历时存在歧义
3. 本地与服务器的数据同步链路没有形成清晰闭环

---

## 3. 今天实际完成的关键工作

### 3.1 清理旧版灌库脚本

已删除旧版脚本：

- `scripts/ingest_resume.py`

原因：

- 该脚本仍使用旧配置命名 `Settings.RESUME_DIR`
- 当前项目已经完全切换到 `self_resume` 体系
- 继续保留只会增加误用风险

当前正式灌库入口统一为：

- `scripts/ingest_self_resume.py`

---

### 3.2 统一部署脚本文案

已更新：

- `scripts/deploy_server.sh`
- `README.md`

统一后的推荐调用方式为：

```bash
bash scripts/deploy_server.sh
```

以及：

```bash
bash scripts/deploy_server.sh --skip-pull
```

这样和当前服务器实践一致，避免因为 `chmod +x` 带来 Git 文件模式变更，被识别为本地修改。

---

### 3.3 README 完成中文版收口

`README.md` 已整体改为中文版，并同步到当前真实流程，包括：

- 当前启动方式
- 依赖安装方式
- 核心接口说明
- Docker / Compose 部署方式
- ACR / 国内镜像源构建说明
- `self_resume` 灌库规则说明

当前 README 已不再描述已经废弃的旧脚本或旧执行方式。

---

### 3.4 `self_resume` 灌库流程标准化

今天最关键的后端工程化收口，是把个人简历灌库流程定义清楚，并落实为正式脚本行为。

当前规则已经明确为：

**`self_resume` 不是历史版本仓库，而是“当前唯一生效的个人简历知识库”。**

因此，`scripts/ingest_self_resume.py` 现在的行为是：

1. 必须显式传入 `--pdf-path`
2. 清空 `data/self_resume/` 中旧 PDF
3. 重建 `data/chroma/self_resume/`
4. 将指定的新 PDF 复制进 `data/self_resume/`
5. 重新灌库并生成当前唯一生效的 `self_resume_chunks`

当前已不再支持以下旧用法：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py
```

也就是说，后续更新简历时的正式流程是：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py --pdf-path <新简历路径>
```

这样做的好处是：

- 行为清晰
- 不会误删目录内“源文件”
- 不会在 `self_resume` 目录里长期残留多份旧 PDF
- 不会继续堆积旧的 Chroma 子目录

---

### 3.5 清理本地 Chroma 顶层历史遗留目录

今天还确认并清理了本地 `data/chroma/` 目录里的旧结构残留。

经过代码检查，当前正式代码只依赖：

- `data/chroma/self_resume/`
- `data/chroma/uploaded_docs/`

不再依赖：

- `data/chroma/` 顶层旧 `chroma.sqlite3`
- `data/chroma/` 顶层历史 UUID 目录

因此已经清理掉这些旧遗留内容，只保留当前标准结构。

现在本地 `data/chroma/` 的顶层目录已经收敛为：

- `self_resume/`
- `uploaded_docs/`

---

## 4. 新版个人简历更新与验证结果

今天已使用新版个人简历完成了真实灌库验证。

本地使用的新简历路径：

```text
D:\ed浏览器下载\AI应用开发工程师-孙雪海.pdf
```

本地真实执行结果：

- 灌库成功
- `Chunks: 4`
- `Collection: self_resume_chunks`

灌库后检查结果：

- `data/self_resume/` 里只保留 1 份当前简历
- `data/chroma/self_resume/` 里只保留本次重建后的当前数据

说明本地新的灌库规则已经真实跑通，不只是测试通过。

---

## 5. GitHub 与服务器同步结果

### 5.1 GitHub 同步

今天这轮代码修改已经提交并推送到当前开发分支：

- 分支：`feature/backend-qa-upgrade`
- 最新提交：`6dd9b5a`

---

### 5.2 服务器代码更新

服务器已执行：

```bash
cd /opt/resume-assistant
git pull
bash scripts/deploy_server.sh --skip-pull
```

结果：

- 服务器成功 fast-forward 到最新提交
- Docker 镜像重建成功
- 容器已重新创建并启动
- `/health` 检查通过
- 部署脚本输出 `Deployment completed.`

说明新版代码已经上线到当前服务器环境。

---

### 5.3 服务器新版简历已同步并灌库

这里需要单独记录，因为这一步和代码更新是两条不同链路。

由于 `data/` 不提交到 GitHub，仅 `git pull` 不会自动把本地最新简历同步到服务器，所以今天额外完成了以下动作：

1. 将新版简历上传到服务器临时目录：

```text
/opt/resume-assistant/tmp/AI应用开发工程师-孙雪海.pdf
```

2. 复制到数据挂载目录下的临时位置：

```text
/opt/resume-assistant/data/tmp/AI应用开发工程师-孙雪海.pdf
```

3. 在运行中的容器内执行新版灌库脚本：

```bash
python scripts/ingest_self_resume.py --pdf-path /app/data/tmp/AI应用开发工程师-孙雪海.pdf
```

服务器真实执行结果：

- 灌库成功
- `Chunks: 4`
- `Collection: self_resume_chunks`

并且已经确认：

- `/opt/resume-assistant/data/self_resume/` 里只有当前新版简历
- `/opt/resume-assistant/data/chroma/self_resume/` 已是重建后的新目录结构

说明当前线上环境的 `self_resume` 数据，也已经切换到新版简历。

---

## 6. 今天新增或收口的项目规则

今天之后，关于工程化流程，项目中应以以下规则为准：

### 规则 1：`self_resume` 只保留当前唯一版本

不再把 `self_resume` 当作历史版本存储区。

### 规则 2：更新个人简历必须显式传 `--pdf-path`

正式命令：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py --pdf-path <pdf路径>
```

### 规则 3：代码更新与数据更新是两条链路

- `git pull` / GitHub 推送更新的是代码
- 简历 PDF 与 Chroma 数据需要单独同步和灌库

### 规则 4：服务器部署脚本统一使用 `bash`

正式命令：

```bash
bash scripts/deploy_server.sh
```

而不是依赖：

```bash
./scripts/deploy_server.sh
```

---

## 7. 当前项目的更准确状态

今天之后，项目状态可以更新为：

**功能可演示、部署链可重复、个人简历知识库更新链已标准化，本地与服务器数据状态已打通。**

这比之前更进一步，因为现在解决的不只是“怎么把代码发上去”，还解决了：

- 新简历怎么安全替换旧简历
- 旧 Chroma 数据怎么清理
- 线上怎么同步最新简历
- 文档、脚本、数据目录约定如何统一

---

## 8. 后续优先方向

在当前基础上，后续更值得继续投入的方向已经比较清楚：

### 优先方向 1：`health / ready / runtime diagnostics` 标准化

当前还可以继续收口：

- 健康检查输出结构
- 就绪检查逻辑
- 模型配置缺失时的错误语义
- 更标准的运行时诊断信息

### 优先方向 2：问答质量继续优化

包括：

- 工作经历抽取稳定性
- 项目总结更像真实面试表达
- 技术栈分类更清晰
- 回答结构更稳定

### 优先方向 3：上传知识库体验优化

包括：

- 更清晰的上传反馈
- 状态提示更明确
- 多文件策略是否支持
- 上传后问答行为是否需要更明确的前端引导

### 优先方向 4：线上验证与 HTTPS 收尾

包括：

- 线上真实问答验证
- 域名接入
- Nginx 最终切换
- HTTPS 配置完成

---

## 9. 一句话总结

今天这轮工作的核心价值，不是新增了某个用户可见功能，而是把“个人简历知识库如何更新、如何同步到服务器、如何避免旧数据残留”这条工程链路真正做实了。

现在项目已经不只是“代码能跑、网站能访问”，而是开始具备了更清晰、更可维护、更适合持续迭代的后端工程化基础。
