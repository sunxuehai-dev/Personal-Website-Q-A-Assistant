# Resume Assistant 开发记录

日期：2026-04-14

## 1. 当前项目阶段

当前项目已经从“功能可演示、部署链已打通”的状态，继续推进到了：

**后端工程化规则进一步收口，个人简历知识库更新流程已标准化，本地与服务器数据状态已同步。**

这意味着项目现在不仅具备：

- 本地运行能力
- GitHub 同步能力
- 服务器 Compose 部署能力
- 双知识库问答能力

还进一步补上了“自有简历如何稳定更新、旧数据如何清理、线上如何同步最新简历”的工程化细节。

---

## 2. 今天的工作重心

今天的工作重点没有继续扩展产品功能，而是集中在后端工程化与标准化收口，主要解决了三类问题：

1. 旧脚本与旧文档入口仍然存在，容易误导后续维护
2. `self_resume` 的灌库流程语义不够严格，更新新简历时存在歧义
3. 本地与服务器的数据同步链路没有形成清晰闭环

---

## 3. 今天实际完成的关键工作

### 3.1 清理旧版灌库脚本

已删除旧版脚本：

- `scripts/ingest_resume.py`

原因：

- 该脚本仍使用旧配置命名 `Settings.RESUME_DIR`
- 当前项目已经完全切换到 `self_resume` 体系
- 继续保留只会增加误用风险

当前正式灌库入口统一为：

- `scripts/ingest_self_resume.py`

---

### 3.2 统一部署脚本文案

已更新：

- `scripts/deploy_server.sh`
- `README.md`

统一后的推荐调用方式为：

```bash
bash scripts/deploy_server.sh
```

以及：

```bash
bash scripts/deploy_server.sh --skip-pull
```

这样和当前服务器实践一致，避免因为 `chmod +x` 带来 Git 文件模式变更，被识别为本地修改。

---

### 3.3 README 完成中文版收口

`README.md` 已整体改为中文版，并同步到当前真实流程，包括：

- 当前启动方式
- 依赖安装方式
- 核心接口说明
- Docker / Compose 部署方式
- ACR / 国内镜像源构建说明
- `self_resume` 灌库规则说明

当前 README 已不再描述已经废弃的旧脚本或旧执行方式。

---

### 3.4 `self_resume` 灌库流程标准化

今天最关键的后端工程化收口，是把个人简历灌库流程定义清楚，并落实为正式脚本行为。

当前规则已经明确为：

**`self_resume` 不是历史版本仓库，而是“当前唯一生效的个人简历知识库”。**

因此，`scripts/ingest_self_resume.py` 现在的行为是：

1. 必须显式传入 `--pdf-path`
2. 清空 `data/self_resume/` 中旧 PDF
3. 重建 `data/chroma/self_resume/`
4. 将指定的新 PDF 复制进 `data/self_resume/`
5. 重新灌库并生成当前唯一生效的 `self_resume_chunks`

当前已不再支持以下旧用法：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py
```

也就是说，后续更新简历时的正式流程是：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py --pdf-path <新简历路径>
```

这样做的好处是：

- 行为清晰
- 不会误删目录内“源文件”
- 不会在 `self_resume` 目录里长期残留多份旧 PDF
- 不会继续堆积旧的 Chroma 子目录

---

### 3.5 清理本地 Chroma 顶层历史遗留目录

今天还确认并清理了本地 `data/chroma/` 目录里的旧结构残留。

经过代码检查，当前正式代码只依赖：

- `data/chroma/self_resume/`
- `data/chroma/uploaded_docs/`

不再依赖：

- `data/chroma/` 顶层旧 `chroma.sqlite3`
- `data/chroma/` 顶层历史 UUID 目录

因此已经清理掉这些旧遗留内容，只保留当前标准结构。

现在本地 `data/chroma/` 的顶层目录已经收敛为：

- `self_resume/`
- `uploaded_docs/`

---

## 4. 新版个人简历更新与验证结果

今天已使用新版个人简历完成了真实灌库验证。

本地使用的新简历路径：

```text
D:\ed浏览器下载\AI应用开发工程师-孙雪海.pdf
```

本地真实执行结果：

- 灌库成功
- `Chunks: 4`
- `Collection: self_resume_chunks`

灌库后检查结果：

- `data/self_resume/` 里只保留 1 份当前简历
- `data/chroma/self_resume/` 里只保留本次重建后的当前数据

说明本地新的灌库规则已经真实跑通，不只是测试通过。

---

## 5. GitHub 与服务器同步结果

### 5.1 GitHub 同步

今天这轮代码修改已经提交并推送到当前开发分支：

- 分支：`feature/backend-qa-upgrade`
- 最新提交：`6dd9b5a`

---

### 5.2 服务器代码更新

服务器已执行：

```bash
cd /opt/resume-assistant
git pull
bash scripts/deploy_server.sh --skip-pull
```

结果：

- 服务器成功 fast-forward 到最新提交
- Docker 镜像重建成功
- 容器已重新创建并启动
- `/health` 检查通过
- 部署脚本输出 `Deployment completed.`

说明新版代码已经上线到当前服务器环境。

---

### 5.3 服务器新版简历已同步并灌库

这里需要单独记录，因为这一步和代码更新是两条不同链路。

由于 `data/` 不提交到 GitHub，仅 `git pull` 不会自动把本地最新简历同步到服务器，所以今天额外完成了以下动作：

1. 将新版简历上传到服务器临时目录：

```text
/opt/resume-assistant/tmp/AI应用开发工程师-孙雪海.pdf
```

2. 复制到数据挂载目录下的临时位置：

```text
/opt/resume-assistant/data/tmp/AI应用开发工程师-孙雪海.pdf
```

3. 在运行中的容器内执行新版灌库脚本：

```bash
python scripts/ingest_self_resume.py --pdf-path /app/data/tmp/AI应用开发工程师-孙雪海.pdf
```

服务器真实执行结果：

- 灌库成功
- `Chunks: 4`
- `Collection: self_resume_chunks`

并且已经确认：

- `/opt/resume-assistant/data/self_resume/` 里只有当前新版简历
- `/opt/resume-assistant/data/chroma/self_resume/` 已是重建后的新目录结构

说明当前线上环境的 `self_resume` 数据，也已经切换到新版简历。

---

## 6. 今天新增或收口的项目规则

今天之后，关于工程化流程，项目中应以以下规则为准：

### 规则 1：`self_resume` 只保留当前唯一版本

不再把 `self_resume` 当作历史版本存储区。

### 规则 2：更新个人简历必须显式传 `--pdf-path`

正式命令：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py --pdf-path <pdf路径>
```

### 规则 3：代码更新与数据更新是两条链路

- `git pull` / GitHub 推送更新的是代码
- 简历 PDF 与 Chroma 数据需要单独同步和灌库

### 规则 4：服务器部署脚本统一使用 `bash`

正式命令：

```bash
bash scripts/deploy_server.sh
```

而不是依赖：

```bash
./scripts/deploy_server.sh
```

---

## 7. 当前项目的更准确状态

今天之后，项目状态可以更新为：

**功能可演示、部署链可重复、个人简历知识库更新链已标准化，本地与服务器数据状态已打通。**

这比之前更进一步，因为现在解决的不只是“怎么把代码发上去”，还解决了：

- 新简历怎么安全替换旧简历
- 旧 Chroma 数据怎么清理
- 线上怎么同步最新简历
- 文档、脚本、数据目录约定如何统一

---

## 8. 后续优先方向

在当前基础上，后续更值得继续投入的方向已经比较清楚：

### 优先方向 1：`health / ready / runtime diagnostics` 标准化

当前还可以继续收口：

- 健康检查输出结构
- 就绪检查逻辑
- 模型配置缺失时的错误语义
- 更标准的运行时诊断信息

### 优先方向 2：问答质量继续优化

包括：

- 工作经历抽取稳定性
- 项目总结更像真实面试表达
- 技术栈分类更清晰
- 回答结构更稳定

### 优先方向 3：上传知识库体验优化

包括：

- 更清晰的上传反馈
- 状态提示更明确
- 多文件策略是否支持
- 上传后问答行为是否需要更明确的前端引导

### 优先方向 4：线上验证与 HTTPS 收尾

包括：

- 线上真实问答验证
- 域名接入
- Nginx 最终切换
- HTTPS 配置完成

---

## 9. 问答助手主链路重构为轻量 Agentic RAG

在完成灌库链路标准化之后，今天还对问答助手本身做了一轮关键重构。

这轮调整的出发点是：

- 旧版问答主链路仍然带有较强的“简历专用分类”色彩
- 旧系统主要依赖固定路由 + 问题分类 + prompt 模板
- 这种方式容易不断堆补丁，不适合继续扩展
- 当前项目更适合“面向小型单文档问答系统”的通用轻量架构

因此，今天正式把问答助手主链路改造成：

**Single-Document Agentic RAG Lite**

---

### 9.1 这轮重构的核心目标

新的问答助手不再围绕“工作经历 / 项目 / 技能 / 教育”这类简历专用问答模板组织，而是围绕更通用的问答决策流程：

1. Analyze
2. Plan
3. Retrieve
4. Synthesize

也就是说，当前系统已经从“简历专用 QA”转向“适配单文档场景的轻量 Agentic RAG”。

这对于当前项目更合理，因为：

- 当前核心知识源本质上就是一份主文档
- 上传 PDF 也需要复用同一套问答范式
- 项目规模较小，不适合直接引入过重的多 Agent 系统
- 但也不应该继续依赖固定模板和单一路径向量检索

---

### 9.2 已删除的旧模块

本轮已删除旧版简历专用核心模块：

- `app/agent/prompts.py`
- `app/agent/questioning.py`
- `app/agent/routing.py`

这些模块的职责已被新的 Agentic RAG 主链路取代。

---

### 9.3 已新增的新模块

本轮新增的核心模块如下：

- `app/agent/models.py`
  - 定义 `QueryAnalysis`、`RetrievalPlan`、`RetrievedEvidence`、`QAResponse`
- `app/agent/analysis.py`
  - 负责对问题做通用分析，输出任务类型、知识源范围、关键词、是否需要 keyword 检索等信息
- `app/agent/planning.py`
  - 根据问题分析结果生成检索计划
- `app/retrieval/keyword.py`
  - 提供轻量关键词检索能力，适合当前小型单文档系统
- `app/retrieval/hybrid.py`
  - 负责 dense 检索与 keyword 检索结果的合并、去重、排序
- `app/agent/synthesis.py`
  - 基于证据进行最终回答生成

同时，`app/agent/service.py` 已被整块重写为新的主 orchestration 层。

---

### 9.4 新问答主链路的结构

当前问答链路已经变成：

```text
用户问题
-> QueryAnalyzer
-> RetrievalPlanner
-> Dense / Keyword Retrieval
-> Hybrid Merge
-> ResponseSynthesizer
-> 最终答案
```

当前支持的通用任务类型包括：

- `fact`
- `summary`
- `locate`
- `compare`

当前支持的知识源范围包括：

- `self_resume`
- `uploaded_docs`
- `both`

这意味着问答系统的决策方式，已经从“简历字段导向”转成“问题类型导向 + 检索策略导向”。

---

### 9.5 这轮重构解决了什么问题

本轮重构带来的直接收益包括：

- 不再需要继续往旧版简历专用分类器里打补丁
- 上传 PDF 问答与个人简历问答开始共用统一的主链路
- 对“个人网站 / 联系方式 / 短字段事实”这类问题，开始具备 keyword 检索兜底
- 后续继续优化检索策略时，不再被旧 prompt 模板体系绑住

也就是说，当前系统已经具备了继续往“更通用、更可维护的小型 Agentic RAG”方向迭代的基础。

---

### 9.6 测试与上线结果

本轮重构后，已同步更新：

- `tests/test_backend_smoke.py`

测试结果：

- `pytest` 通过

说明：

- 新的轻量 Agentic RAG 主链路已经通过本地 smoke tests
- 上传问答、自有简历问答、灌库脚本相关基础行为没有被重构破坏

本轮代码已提交并推送到 GitHub：

- 分支：`feature/backend-qa-upgrade`
- 提交：`3eac4ba`

服务器也已完成：

```bash
cd /opt/resume-assistant
git pull
bash scripts/deploy_server.sh --skip-pull
```

并确认：

- 容器重建成功
- 服务启动成功
- 健康检查通过

最后已完成线上人工验证：

**新版问答助手测试成功。**

这说明当前这版轻量 Agentic RAG 已经真实部署到服务器，并可进入下一轮效果调优阶段。

---

## 10. 问答助手流式输出优化

在轻量 Agentic RAG 主链路上线验证通过之后，今天还继续对问答助手的前端交互体验做了一轮优化。

本轮的目标是：

**让网站中的问答助手从“整段一次性显示”改为“更接近常见 AI 助手的逐步生成体验”。**

---

### 10.1 为什么需要这轮优化

原来的页面行为是：

- 用户提交问题
- 前端等待 `/chat` 完整返回
- 最后一次性把整段答案插入聊天窗口

这种交互虽然功能上没有问题，但用户体感比较差，尤其在现在问答链路已经升级为 Agentic RAG 之后，检索与生成阶段更明显，等待时缺少反馈。

因此这轮优化的重点不在回答内容本身，而在于“答案如何逐步呈现给用户”。

---

### 10.2 后端流式输出已接入

本轮新增了新的接口：

- `POST /chat_stream`

对应改动：

- `app/api/routes.py`
- `app/agent/service.py`
- `app/agent/synthesis.py`

当前实现方式为：

- 仍然复用当前的轻量 Agentic RAG 主链路
- 在 synthesis 阶段改为流式返回 token/chunk
- 最终通过 `StreamingResponse` 以 SSE 风格向前端持续输出

这意味着问答助手现在已经具备真正的流式传输能力，而不再是“后端先全部生成完，再统一返回”。

---

### 10.3 流式问题的真实瓶颈与定位结果

这轮优化过程中，出现了一个很关键的排查点：

虽然 FastAPI 后端已经在流式返回，但网页上最开始看起来仍然像“一次性输出”。

经过排查后确认：

- 问题不在 FastAPI
- 问题不在 Agentic RAG 主链路
- 问题也不在模型是否支持流式
- 关键瓶颈在于 **Nginx 反向代理层的响应缓冲**

也就是说：

- 容器内请求 `/chat_stream` 时，后端确实已经分块返回
- 但公网访问链路经过 Nginx 时，Nginx 会缓冲响应
- 最终浏览器看到的效果仍然像一次性输出

这一步的定位非常重要，因为它说明“应用层已经流了”，但“代理层还没允许它真正以流的形式到达浏览器”。

---

### 10.4 Nginx 已针对 `/chat_stream` 做流式代理配置

为解决上述问题，已调整服务器 Nginx 配置，对 `/chat_stream` 单独做了非缓冲代理处理。

核心配置思路包括：

- `proxy_buffering off`
- `proxy_cache off`
- `chunked_transfer_encoding on`
- `proxy_read_timeout 300s`

这样做的效果是：

- 只有流式接口关闭代理缓冲
- 普通接口继续使用标准代理行为
- 不会为了流式问答而影响整个站点的普通请求处理方式

当前 Nginx 配置已成功 reload，并已验证公网链路能够分块返回 `/chat_stream` 的内容。

---

### 10.5 前端已改成逐字渲染

除了后端流式和 Nginx 配置之外，还进一步对前端渲染做了增强。

对应文件：

- `static/scripts/site.js`

当前前端行为已经变成：

1. 优先调用 `/chat_stream`
2. 边接收流式 chunk 边更新聊天窗口
3. 对收到的 chunk 再做“打字机式逐字渲染”
4. 如果流式接口失败，自动 fallback 到旧的 `/chat`

这一层非常关键，因为即使后端已经在流式输出，模型流出来的粒度也可能是“短词/短句块”，并不总是天然表现成“一个字一个字出现”。

前端增加逐字渲染后，最终视觉效果已经更接近常见 AI 助手的输出方式。

---

### 10.6 测试与上线结果

本轮已补充测试：

- `tests/test_backend_smoke.py`
  - 新增 `/chat_stream` 的 smoke test

当前测试结果：

- `pytest` 通过

本轮代码已提交并推送：

- 提交：`f2e109e`
  - 后端流式输出主逻辑
- 提交：`dfaddec`
  - 前端逐字渲染优化

服务器已完成更新部署，并最终确认：

- `/chat_stream` 已上线
- Nginx 流式代理配置已生效
- 前端逐字渲染已上线
- 线上人工测试成功

最终结果是：

**问答助手现在已经具备“后端流式返回 + 代理层不缓冲 + 前端逐字显示”的完整链路。**

---

## 11. 一句话总结

今天这轮工作的核心价值，不只是把“个人简历知识库如何更新、如何同步到服务器、如何避免旧数据残留”这条工程链路做实了，还进一步把问答助手主链路从旧版简历专用 QA 正式重构成了适配单文档场景的轻量 Agentic RAG。

现在项目已经不只是“代码能跑、网站能访问”，而是开始具备了更清晰、更可维护、更适合持续迭代的后端工程化与问答系统基础，并且连用户侧的问答交互体验也完成了一轮重要升级。
---

## 12. 当前问答助手模块最终状态

经过今天这一轮连续重构、线上验证和体验收口，当前项目中的问答助手模块已经进入一个相对稳定、可继续迭代的状态。

### 12.1 当前问答助手的总体形态

当前问答助手不再使用早期那种“强路由 + 多分支显式切换”的结构，而是收敛成：

**统一问答助手 + 本地 RAG 上下文增强 + Qwen 按需联网 + 单次重试**

也就是说，现在的系统核心思想是：

1. 先判断当前问题与本地资料的相关度
2. 如果相关，则先检索本地知识库并提供证据
3. 最终统一交给同一个 Qwen 回答器处理
4. Qwen 调用时统一开启 `enable_search=true`
5. 是否真正利用联网信息，由模型自行判断
6. 当高相关问题首次检索效果较弱时，只允许进行一次“问题重写 + 扩检索”重试

这套方案比此前的显式 `local_rag / web_search / hybrid` 主流程更简单，也更符合当前这个个人网站项目的真实需求。

### 12.2 当前已确认有效的能力边界

当前版本已通过真实测试确认：

- 关于站长本人简历、项目、技能、经历、个人网站的问题，回答效果已达到当前满意水平
- 上传 PDF 问答链路已恢复，且在“启用上传文档问答”时，默认优先查询上传文档知识库
- 当问题明显属于外部最新信息时，Qwen 可以在统一回答过程中结合联网能力补充信息
- 前端不再显示“结合本地资料 / 结合联网信息”等来源标签，页面观感更自然
- 问答助手最终回答阶段已经改成真正流式输出，不再是整段生成完成后一次性返回

### 12.3 当前问答助手的实际主链路

当前链路可以概括为：

```text
用户提问
-> LocalRelevanceJudge
-> 可选本地检索（self_resume / uploaded_docs）
-> Qwen 统一回答（enable_search=true）
-> AnswerEvaluator
-> 必要时单次 RetryRewriter + 扩检索
-> 最终答案
```

其中：

- `LocalRelevanceJudge`：负责判断本地资料是否值得参与
- `QueryAnalyzer + RetrievalPlanner`：负责本地检索范围与策略
- `ResponseSynthesizer`：负责统一组织最终回答
- `AnswerEvaluator`：只负责判断是否需要一次补救重试
- `RetryRewriter`：只在必要时重写检索问题并扩大召回

### 12.4 当前版本的重要工程结论

今天这轮优化之后，问答助手模块已经形成了几个明确结论：

1. 不再继续维护复杂的强路由架构  
当前项目规模不需要显式维护多条主要回答分支，统一回答器更适合当前产品形态。

2. 本地 RAG 的角色已经明确  
本地检索不是独立产品功能分支，而是问答回答时的上下文增强能力。

3. 联网能力的接入方式已经明确  
当前不再单独维护独立的联网搜索主模块，而是在 Qwen 统一回答时使用 `enable_search=true`。

4. 上传文档问答策略已经明确  
当用户开启上传文档问答且上传文件存在时，默认优先检索上传文档，而不是个人简历。

5. 前端交互层已经收口  
页面当前只呈现答案本身，不再暴露内部来源标签；流式输出体验已经完成第一轮优化。

### 12.5 当前模块仍然存在但可接受的限制

虽然当前整体效果已经满意，但仍有几个后续可以继续优化的点：

- 首字节时间仍然会受到“相关度判断 + 本地检索”前置阶段影响
- Qwen 是否实际联网，目前仍然是基于问题特征和回答结果做轻量推断，而不是严格可观测信号
- 超短寒暄问题的回答风格后续仍可继续微调，让它更自然、更像真实助手
- 多轮上下文记忆目前没有专门展开，当前仍以单轮问答为主

这些问题不会影响当前版本可用性，但可以作为后续体验优化方向。

### 12.6 当前问答助手模块状态结论

截至当前，项目中的问答助手模块已经完成从“简历专用问答”到“个人网站统一问答助手”的关键收口，当前可将其状态定义为：

**功能可用、体验基本达标、链路已上线、后续以性能与细节体验优化为主。**
