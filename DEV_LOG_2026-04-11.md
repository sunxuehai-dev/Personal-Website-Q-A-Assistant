# Resume Assistant 开发记录
日期：2026-04-11

## 项目阶段
当前项目已经从“简历问答 demo”推进到“个人网站 + 内嵌个人问答助手”的阶段。

稳定基线：
- `main` 分支保留为可运行版本

持续开发分支：
- `feature/backend-qa-upgrade`

---

## 今天已经完成的关键工作

### 1. 拆分知识库
知识库已经拆成两套：

- `self_resume`
  - 存放你自己的个人简历
- `uploaded_docs`
  - 存放用户在前端上传的 PDF

对应目录：

- `data/self_resume/`
- `data/uploads/`
- `data/chroma/self_resume/`
- `data/chroma/uploaded_docs/`

对应 collection：

- `self_resume_chunks`
- `uploaded_doc_chunks`

---

### 2. 支持个人简历单独灌库
已经新增脚本：

- `scripts/ingest_self_resume.py`

作用：

- 手动把后端目录中的个人简历 PDF 灌入 `self_resume`
- 以后你只要替换新的简历 PDF，再执行一次脚本即可

---

### 3. 问答路由支持双知识库
已经实现路由器：

- `app/agent/routing.py`

支持的目标：

- `self_resume`
- `uploaded_docs`
- `both`

当前逻辑：

- 如果当前会话没有启用上传文档，默认只查 `self_resume`
- 如果当前会话上传了文件，再根据问题内容判断查哪一侧
- 当问题包含对比、综合、结合等语义时，可以走 `both`

---

### 4. 修复“历史上传污染新会话”的问题
之前的问题：

- 只要 `data/uploads/` 里残留 PDF，新页面也可能错误地走上传知识库

现在的策略：

- 前端会显式传 `use_uploaded_docs`
- 只有当前会话真的上传过文档，才启用上传知识库

涉及文件：

- `app/api/routes.py`
- `app/agent/service.py`
- `static/scripts/site.js`

---

### 5. 完成个人网站形态
前端已经不再依赖 Gradio 作为主界面。

当前网站技术栈：

- `FastAPI`
- `Jinja2`
- `HTML/CSS/JS`

页面入口：

- `GET /`

主要页面内容：

- Hero 首页介绍
- 关于我
- 经历
- 项目
- 技能
- 内嵌问答助手

---

### 6. 完成第一轮前端视觉优化
已经完成的视觉调整：

- 缩小首页姓名标题
- 强化“与我对话”按钮
- 接入个人照片
- 重构 Hero 右侧信息卡
- 调整左右版式平衡
- 问答区保留上传能力，但默认界面更简洁

当前结论：

- 前端已经达到“可演示、观感不错”的程度
- 后续优先级应转向后端问答质量和部署能力

---

### 7. 本轮新增后端增强
本轮新完成的重点是把后端从“能用”往“更稳、更接近上线”推进。

新增内容：

- 问题分类器：`app/agent/questioning.py`
- 更稳定的回答 prompt：`app/agent/prompts.py`
- 问答服务增强：`app/agent/service.py`
- 上传知识库管理器：`app/uploads/service.py`
- 上传状态接口：
  - `GET /upload_status`
  - `DELETE /upload_status`

效果：

- 可以区分工作经历、项目、技能、教育、自我总结、综合对比等问题类型
- 上传文档状态可以被前端感知
- 前端可以清空当前上传知识库

---

### 8. 修复 Windows 下上传灌库锁文件问题
发现的问题：

- 上传新 PDF 前，系统会清空旧上传知识库
- 之前的实现会尝试直接删除 Chroma 底层 sqlite 文件
- Windows 下 sqlite 文件被占用时会触发 `WinError 32`

修复方式：

- `app/uploads/service.py` 只删除上传 PDF 和 collection
- 不再把物理删除 `chroma.sqlite3` 当作成功条件

结果：

- 上传重置逻辑在 Windows 下更稳定

---

### 9. 修复路由器和分类器关键词乱码
发现的问题：

- `app/agent/routing.py`
- `app/agent/questioning.py`

里面的中文关键词曾出现乱码，导致：

- 问“工作经历”可能无法正确分类
- 问“上传的 PDF”可能无法正确路由

修复方式：

- 重写两个文件
- 统一使用稳定的 UTF-8 / Unicode 字符串

结果：

- 关键词判断恢复正常

---

### 10. 补充无网络 smoke tests
新增测试文件：

- `tests/test_backend_smoke.py`

测试覆盖：

- 上传知识库重置不会再误删 Chroma sqlite 文件
- API 基础链路在 mock 模型下可跑通
- `/health`
- `/upload_status`
- `/upload_resume`
- `/chat`
- `DELETE /upload_status`

意义：

- 以后改后端逻辑时，有一层基础回归保障

---

## 当前项目结构重点

后端核心：

- `app/main.py`
- `app/api/routes.py`
- `app/agent/service.py`
- `app/agent/routing.py`
- `app/agent/questioning.py`
- `app/agent/prompts.py`
- `app/uploads/service.py`
- `app/ingestion/pipeline.py`
- `app/retrieval/`

前端核心：

- `templates/index.html`
- `static/styles/site.css`
- `static/scripts/site.js`

部署相关：

- `Dockerfile`
- `.dockerignore`
- `README.md`

---

## 目前已经具备的能力

- 个人简历持久化知识库
- 手动重灌个人简历
- 上传 PDF 自动灌库
- 双知识库路由
- 问题类型分类
- 网站首页展示
- 网站内嵌问答助手
- 上传状态管理
- 基础部署文件

---

## 仍然值得继续优化的方向

### 1. 后端问答质量
还可以继续提升：

- 更稳定地抽取工作经历
- 更像面试表达的项目总结
- 技术栈分组输出
- 自我介绍风格优化

### 2. 上传知识库体验
还可以继续增强：

- 多文件策略
- 文件替换反馈
- 更明确的前端状态提示

### 3. 部署上线
下一步可以往真正上线版本推进：

- Docker 部署验证
- 云服务器部署
- 反向代理
- 域名和 HTTPS

---

## 明天继续开发时建议的起点
建议优先继续做后端，不要先回到纯视觉微调。

推荐顺序：

1. 跑一遍当前分支
2. 验证个人问答和上传问答
3. 继续优化回答质量和部署细节

---

## 启动方式
后端网站：

```bash
.venv\Scripts\python -m app.main
```

访问：

```text
http://127.0.0.1:8008/
```

个人简历灌库：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py
```

---

## 一句话总结
当前分支已经具备“可演示、可继续打磨、接近部署上线”的基础形态，后续重点应该放在后端问答质量、上传文档体验和上线部署验证。
