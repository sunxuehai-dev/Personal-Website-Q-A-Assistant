# Resume Assistant 开发记录

日期：2026-04-11

## 今日目标

今天的核心目标是把项目从“可运行的简历问答 demo”推进到“个人网站内嵌问答助手”的阶段，并同时稳定后端知识库结构与问答路由逻辑。

---

## 已完成的关键工作

### 1. 重构知识库结构

原始状态：
- 只有单一简历目录
- 只有单一 collection
- 前端上传和个人简历共用一套逻辑

现在的结构：
- `data/self_resume/`
  - 存放孙雪海自己的个人简历 PDF
- `data/uploads/`
  - 存放前端上传的 PDF
- `data/chroma/self_resume/`
  - 个人简历的向量库
- `data/chroma/uploaded_docs/`
  - 上传文档的向量库

配置位置：
- `app/core/config.py`

当前 collection：
- `self_resume_chunks`
- `uploaded_doc_chunks`

---

### 2. 新增个人简历独立灌库脚本

已新增脚本：
- `scripts/ingest_self_resume.py`

作用：
- 手动灌入个人简历知识库
- 便于以后替换新版简历 PDF 后重新灌库

使用方式：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py
```

或者：

```bash
.venv\Scripts\python scripts\ingest_self_resume.py --pdf-path path\to\your_resume.pdf
```

当前状态：
- 已成功灌入 `data/self_resume/AI应用开发工程师-孙雪海.pdf`
- 灌库结果为 4 个 chunk

---

### 3. 后端问答路由改造成双知识库

已新增：
- `app/agent/routing.py`

当前支持的路由目标：
- `self_resume`
- `uploaded_docs`
- `both`

当前逻辑：
- 如果当前会话没有启用上传文档，默认只走 `self_resume`
- 如果当前会话上传过文件，则根据问题内容判断：
  - 问本人简历
  - 问上传文档
  - 问两者结合

当前已经验证的路由行为：
- “请介绍孙雪海的工作经历” -> `self_resume`
- “请根据上传的文件介绍候选人的工作经历” -> `uploaded_docs`
- “请综合我的简历和上传文件，总结工作经历” -> `both`

---

### 4. 修复“上传文档污染默认问答”的异常状态

问题：
- 之前后端只要磁盘里存在上传 PDF，新的页面会话也可能走上传知识库
- 这会破坏“默认问我自己”的产品逻辑

解决方案：
- `POST /chat` 新增字段：
  - `use_uploaded_docs: bool = False`
- 前端页面首次打开时默认传 `false`
- 只有当前页面真的上传过 PDF，前端才会切换为 `true`

涉及文件：
- `app/api/routes.py`
- `app/agent/service.py`
- `static/scripts/site.js`

结果：
- 之前遗留在上传目录里的 PDF 不会再影响新会话默认路由

---

### 5. 从 Gradio 过渡到真正的网站前端

当前最终前端已经不再依赖 Gradio 页面展示，而是走：
- `FastAPI + Jinja2 + HTML/CSS/JS`

新增部分：
- `app/web/routes.py`
- `templates/index.html`
- `static/styles/site.css`
- `static/scripts/site.js`

首页入口：
- `GET /`

当前网站模块：
- Hero 首屏
- About
- Experience
- Projects
- Skills
- Ask My Resume 问答区

---

### 6. 完成第一轮前端视觉精调

今日已经完成的设计优化包括：

#### 首屏
- 缩小“孙雪海”字号
- 提高“与我对话”按钮权重，作为主 CTA
- 将“查看项目”降为次级按钮
- 接入个人照片
- 删除原来右侧黑色控制台
- 首屏改为“照片 + 简洁资料卡”
- 左侧新增两张价值说明小卡

#### 问答区
- 保留上传 PDF 自动灌库
- 不显示检索片段
- 状态提示保留轻量展示
- 新增 3 个示例问题 chips

#### 编码稳定性处理
为了避免 Windows / PyCharm 再次把中文源码搞乱：
- 模板中的静态中文大量改用 HTML 实体
- JS 中的静态中文改用 `\uXXXX`

这不是错误，而是为了编码稳定性做的工程处理。

---

## 当前项目状态

### 后端

当前已经具备：
- 个人简历独立知识库
- 上传文档独立知识库
- 双知识库路由
- 默认个人简历问答
- 上传 PDF 自动灌库
- API 可正常工作

关键接口：
- `GET /`
- `GET /health`
- `POST /upload_resume`
- `POST /chat`

### 前端

当前网站可用：
- 首页可打开
- 样式和脚本可加载
- 头像已接入：
  - `static/images/profile.jpg`
- 问答区可用
- 上传 PDF 后自动灌库

---

## 今日修改过的重要文件

### 后端核心
- `app/core/config.py`
- `app/ingestion/loader.py`
- `app/ingestion/pipeline.py`
- `app/retrieval/vectorstore.py`
- `app/retrieval/retriever.py`
- `app/agent/routing.py`
- `app/agent/service.py`
- `app/api/routes.py`
- `scripts/ingest_self_resume.py`

### 网站前端
- `app/main.py`
- `app/web/routes.py`
- `templates/index.html`
- `static/styles/site.css`
- `static/scripts/site.js`

### 其他
- `requirements.txt`
- `README.md`

---

## 当前仍然存在但尚未优化的点

### 1. 后端问答质量还有提升空间

虽然现在已经能回答，但还可以更成熟，尤其是：
- 工作经历问题
- 技术栈总结
- 项目经历结构化输出
- 自我优势总结

目前还没有做“问题分类器 + 结构化回答模板”。

这是下一步最值得做的后端优化方向。

### 2. 上传知识库生命周期还可以继续完善

当前已经解决“新会话默认不受旧上传文件影响”的问题，但还没做：
- 清空上传知识库接口
- 查看当前上传文档状态接口
- 只保留最近一次上传文件的策略

### 3. 前端仍可继续精修

目前用户感觉“总体还不错”，因此前端先停在这个阶段是合理的。
如果后面继续优化，建议优先级是：
- 项目区层级
- 问答区产品感
- 文案精修
- 动效细节

---

## 明天建议的开发起点

优先从后端开始，不再继续前端视觉微调。

推荐下一步：

### 方案 A：问题分类器 + 结构化回答器

目标：
- 识别问题类型
- 按不同问题类型生成更稳、更像面试表达的答案

建议分类：
- `work_experience`
- `projects`
- `skills`
- `education`
- `self_summary`
- `general`

然后根据类型套不同回答模板。

这是最推荐的下一步。

### 方案 B：上传知识库管理

补充以下能力：
- 清空上传知识库
- 查看当前上传文档
- 会话级上传文档状态增强

如果明天更关注工程完整性，可以先做这个。

---

## 明天继续开发时建议先做的事

1. 先看这个文档
2. 启动项目确认当前状态

启动方式：

```bash
.venv\Scripts\python -m app.main
```

然后打开：

```text
http://127.0.0.1:8008/
```

3. 继续从“后端问答助手优化”开始，而不是先改界面

---

## 一句话总结

今天已经把项目从“简历问答 demo”推进成了“个人网站 + 双知识库问答助手”的形态，并解决了默认路由、独立灌库、网站接入和一轮前端重构问题。下一步应重点优化后端问答质量，而不是继续大幅调整前端。
